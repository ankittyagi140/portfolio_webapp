/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [],
  },
  // Enable experimental features if needed
  experimental: {
    // Add any experimental features here
  },
  // Transpile shared folder if needed
  transpilePackages: [],
};

export default nextConfig;

